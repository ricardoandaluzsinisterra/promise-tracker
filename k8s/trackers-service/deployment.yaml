apiVersion: apps/v1
kind: Deployment
metadata:
  name: trackers-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trackers-service
  template:
    metadata:
      labels:
        app: trackers-service
        version: v1
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              until nc -z postgres-trackers 5432; do echo "waiting for postgres-trackers"; sleep 2; done
              until nc -z kafka 9092; do echo "waiting for kafka"; sleep 2; done
      containers:
        - name: trackers-service
          image: trackers-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          env:
            - name: KAFKA_BROKER
              value: "kafka:9092"
