apiVersion: apps/v1
kind: Deployment
metadata:
  name: trackers-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trackers-service
  template:
    metadata:
      labels:
        app: trackers-service
        version: v1
    spec:
      containers:
        - name: trackers-service
          image: trackers-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          env:
            - name: KAFKA_BROKER
              value: "kafka:9092"
